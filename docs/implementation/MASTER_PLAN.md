# Latchway implementation master plan

The authoritative execution contract defines twenty sequential phases. Work should be delivered as reviewable vertical slices; a later phase may proceed only where it does not depend on an unmet earlier gate. Missing external credentials do not block independent work.

| Phase | Objective | Current state | Gate summary |
| --- | --- | --- | --- |
| 0 | Audit all repositories and classify existing work | Complete; governance baselines committed | Baseline complete and tracked trees clean at recorded passing revisions |
| 1 | Governance, protocol contracts, threat model, ADRs | Draft contract `0.4.0`/protocol `1` is immutable at core `c9347421fac4c729f20ea87f9205c66c15fa983f`; deterministic bundle and all four SDK locks are synchronized | Core schemas, deterministic bundle, and SDK lock/fixture equality validate; live compatibility remains separate |
| 2 | Go, PostgreSQL, CLI, dashboard and image foundation | Runnable foundation, prior Compose gate, and current local non-root OCI build pass; current Compose smoke and registry digest remain open | Local runtime/image gates pass; release image evidence is absent |
| 3 | Database and one-time administrative bootstrap | Canonical Admin API, CLI, embedded owner setup/login, write-only secret lifecycle, and schema version 11 locally validate | Exactly one owner can bootstrap; implemented mutations are audited; fresh and upgrade migrations pass |
| 4 | Immutable configuration revisions | Revision creation, validation, planning, conflict-safe activation, rollback, and compiled snapshots committed and locally tested | Local immutable-revision and concurrency gates pass |
| 5 | Identity verification and normalized principals | Strict verifier/JWKS/privacy/user store is wired into active configuration and session exchange | Local identity, rotation, mapping, policy, and adversarial gates pass |
| 6 | RFC 9449 DPoP and session vertical slice | Local debug-attested challenge/exchange, access/refresh issuance, replay, JWKS, protected authorization, reuse detection, and current-installation revocation committed | Full local Phase 6 and public protected-revoke gates pass; production native trust is outside this phase evidence |
| 7 | First end-to-end proxy | Local authenticated debug-attested OpenAI Chat vertical passes through deterministic mock upstream; local-verifier CLI and live canary remain | Local authenticated request, relay, settlement and replay-without-redispatch gate passes |
| 8 | Quota, pricing and usage settlement | Atomic request/output/input/total/cost reservations, request/input/output/total token buckets, per-request input/output/total bounds, output clamps, concurrency leases, configured pricing, usage provenance, snapshots, and user overrides have authenticated PostgreSQL proof. Hard input/total limits and nonzero input-priced hard cost activate only through an exact-model, restricted text-only Chat preflight whose body and bounds are independently rebound before reservation and dispatch. Unknown or over-bound usage charges conservatively. | Remaining multi-attempt/retry-cost work, other cost algorithms, upstream-reported pricing, and physical-scope snapshots remain open |
| 9 | Protocol adapters and routing | Initial OpenAI Chat adapter and priority/weighted/sticky selection primitives exist; multi-attempt persistence and endpoint-correct registry are in progress, while Responses, Embeddings, Anthropic, restricted opaque dispatch, fallback/retries, timeouts and circuits remain | OpenAI, Anthropic and restricted opaque routes conform with deterministic fallback and attempt accounting |
| 10 | Apple App Attest and Swift SDK | Swift SDK and contract `0.4.0` lock/fixtures pass 49 tests and package/build gates; live conformance, server trust-root verification and physical proof remain | Fixtures and physical-device validation pass |
| 11 | Play Integrity and Android SDK | Android contract `0.4.0` lock/fixtures and hardened process-wide session/key lifecycle pass 670 Gradle tasks and 68 tests; server verification, publication, and Play-track proof remain | Fixtures and Play-distributed validation pass |
| 12 | Browser/Node JavaScript SDK | SDK contract `0.4.0` lock/fixtures and local browser/Node/package/reproducibility gates pass; live conformance remains | Browser and Node conformance pass against the selected core |
| 13 | React Native bridge SDK | Native-backed SDK contract `0.4.0` lock/fixtures and local source/package/native-boundary gates pass; native-consumer/published-native-dependency and device conformance remain | Native dependency and example-app conformance pass |
| 14 | Complete Admin API, CLI and dashboard | Bootstrap/auth/tenant/config/API-token/user-override and canonical write-only secret API/CLI slices are committed; audit/usage/request views, wizard, self-tests and remaining resources are pending | All control planes use one API and audit every mutation |
| 15 | Observability and PostgreSQL jobs | Initial bounded quota-expiry and DPoP-replay cleanup runtime is role-aware; broader telemetry/jobs remain | Metrics, traces, health and complete crash recovery remain open |
| 16 | Deployment assets | Foundation exists; current image, cloud smoke, and operational deployment evidence remain | Compose and documented cloud smoke tests pass |
| 17 | Cross-repository conformance | Contract `0.4.0` lock/fixture synchronization is complete across all four SDKs; the latest-core live matrix is open | Shared vectors and real proxied requests pass for every SDK |
| 18 | Security, race, fuzz, load and upgrade hardening | Current core checkpoint passes full PostgreSQL normal/race, vet, ten fuzz targets, deterministic bundle, console and independent P0-P2 review; Android mobile lifecycle fixes pass all 670 Gradle tasks; load/soak/upgrade/scans remain open | Security and reliability targets have recorded evidence |
| 19 | Documentation and version 1.0 release | Evidence ledgers are maintained; no release artifacts, tags, or publications exist | Every Definition of Done item links to post-build evidence |

Schema version 11 retains a bounded Phase 8 recovery limitation inherited from schema 9: an expired per-request-only entryless attempt cannot reconstruct its applied cap or add an unknown-output usage row. That shape has no durable capacity to recover; normal known settlement persists provider usage.

The contract-`0.3.0` secret-lifecycle checkpoint at core `05f88b41813c210a23a459519abd3f7a9c3e45fa` implements write-only Admin metadata/list/create/rotate/delete operations, exact current-version concurrency, encrypted version history, permanent tombstones, schema-defined configuration reference protection, bounded valid-UTF-8 inputs, redacted CLI and audit behavior, and truthful ambiguous-commit correlation. Full PostgreSQL normal/race suites, vet, contract validation, all eight fuzz targets, console gates and two byte-identical bundles at SHA-256 `ea265cfa750df8faeeaeac7bc60c04c4d907384205b5bf4d78a22a79dfc4d24c` pass. JavaScript `79dee67fcacad2fc78bca56682ddacca12bba383`, iOS `173b5d7d53222160b5f7d835d238b4f04eb1e04e`, Android `a732120575b3c844abe926cc007678665a8b7b80`, and React Native `548e2b79e6ad96b574c431d3ca1abee360f9a0cc` pin its exact bundle and pass their local gates. This synchronized checkpoint is not a release or live-compatibility claim.

The contract-`0.4.0` trusted-input checkpoint at core `c9347421fac4c729f20ea87f9205c66c15fa983f` activates hard UTC-calendar input/total-token limits and nonzero input-priced hard cost only for a restricted, exact-physical-model OpenAI Chat profile. The adapter and data plane bind and independently verify the rewritten body, profile, model, framing formula, and input/output/total bounds before reservation and again before target acquisition; quota replay and recovery bind the same proof, and anomalous provider usage settles conservatively. Full PostgreSQL normal/race suites, vet, contract validation, all ten fuzz targets, console gates, byte-exact generated SQL, two byte-identical bundles at SHA-256 `39d32a2c9e4b0381ff815a40d87d75b51e4f37d6de55121b7bb0beef690c5c59`, authenticated malicious-underbound/body-tamper E2E, and independent P0-P2 review pass. JavaScript `e2d69505d0bba796ac6129e528b640dccb917b1c`, iOS `922347286157f15ad24785ac735861c6455c2e0e`, Android `652f14e9fd1fa6b8f60bdb3c419d4d6b0f526840`, and React Native `dcca2e2a9070af95bcfd4babe0cef6677487cd5c` pin the exact bundle and pass their local gates. This synchronized checkpoint is not a release or live-compatibility claim.

The bounded token-bucket milestone passes at core `3f9a8496ca1ec141cc289ecd40d93c5b88862b29` from output-token quota-store `294c6e86c156178f583d4bbd9c1936942ece1e46`, configuration `4513b826be8b2cdc14dbd1186a64d20af2bf2d5a`, and dataplane/E2E `3f9a8496ca1ec141cc289ecd40d93c5b88862b29`, building on the request-token slice. One semantic request is exactly `1,000,000,000,000` balance quanta in a `rolling` bucket, while an output-token reservation debits the exact adapter-applied cap. The static output clamp is the minimum of the feature absolute maximum, every applicable per-request maximum, and every applicable token-bucket capacity, independent of rule order and current balance. Refill credits only complete `1us` PostgreSQL ticks and exact retry timestamps remain representable beyond a `time.Duration` horizon. All token buckets keep `used_units=0` and `reserved_units=0`. A known successful measured output refunds only reserved-minus-actual units; equal usage avoids a bucket write and zero usage refunds in full. Failures, unknown usage, and dispatched expiry retain the full debit; pre-dispatch release and undispatched expiry refund it. Refunds saturate at the current capacity after a policy decrease, and public `Retry-After` calculation safely caps at `MaxInt32` seconds. Full PostgreSQL normal/race suites, full vet and contract validation, both changed configuration fuzz targets for 5 seconds, focused output-token store and authenticated E2E count-3 repetitions and race runs, deterministic byte-identical bundle builds at unchanged SHA-256 `74fc7ada8d835d46b25f763a703b79003cdc8243d6f4b2509645e5a82367ab12`, zero `api`/migration diff, and an independent clean P0-P2 review pass. The contract remains `0.1.0`/wire `1`, database schema remains 9, and SDK locks remain at the prior synchronized baseline; this is bounded local implementation evidence, not release readiness or live provider/native-attestation proof.

The authenticated quota-snapshot milestone passes at core `3fcc0400cc9da7def1a6ad0d54718d7f5b62674e`, split across policy projection `4f8ccf55b5c6e3d2113e319fb3ab1c279b0037d1`, read-only store projection `0eb7345837865aae3fd8541d0c31a7b20946ac8d`, client transport `75d96b9e252f17e8b80640c6cd6572ffffade02b`, and dataplane/E2E aggregate `3fcc0400cc9da7def1a6ad0d54718d7f5b62674e`. It supports only hard OpenAI Chat access/limit plans stable across streaming facts and rejects route-, upstream-, and model-scoped rules without a selected physical route. The store verifies revision and obtains one database time inside a repeatable-read, read-only transaction; batches existing bucket reads; projects missing state as pristine; and does not lock, write, or materialize quota state. Calendar, token, per-request, and concurrency projections preserve their distinct semantics; token `used` is availability-derived after virtual exact refill rather than cumulative historical usage. The bodyless canonical GET consumes DPoP replay state for authorization, but the quota counters and request/reservation/attempt/usage state remain unchanged. Full PostgreSQL normal/race suites, full vet, policy fuzzing, contract validation, focused snapshot PostgreSQL/authenticated E2E normal and race gates, byte-identical unchanged contract bundles, zero `api`/migration diff, and independent clean P0-P2 review pass. Contract `0.1.0`/wire `1`, schema 9, bundle hash, and SDK locks are unchanged.

The bounded hard-cost milestone passes at core `6304b655d0e2690cbe154b4a66c4ec87966f4387`, split across quota lifecycle `925cb91fa9e96e459ef476aefd2bce8822e9c224`, configuration activation/runtime compilation `5033d0db1faa15ae687b9bcde3e65b2631376950`, and data-plane/client transport plus authenticated E2E aggregate `6304b655d0e2690cbe154b4a66c4ec87966f4387`. Only hard `cost_nano_usd/calendar` activates, and only for routes with configured USD request/output pricing and zero input-token price. The exact checked reservation is the request charge plus the separately rounded output charge at the server-clamped maximum, identical across every applicable cost rule, including zero. Known calculated cost settles actual units and refunds unused capacity; unknown post-dispatch cost and provider results beyond the trusted cap consume the full reservation without creating a false known-cost row. Predispatch and undispatched expiry paths refund fully, while dispatched expiry charges fully. Recovery validates revision and an independent catalog binding, terminal replay validates the exact settled/released split, and historical priced non-cost rows retain their original reservation-key format. Contention, mixed-rule denial, corruption, expiry, replay, authenticated predispatch denial, and public snapshot state pass normally and under the race detector. Full PostgreSQL normal/race suites, full vet, configuration/policy fuzzing, contract validation, byte-identical unchanged bundles, zero `api`/migration/generated-database diff, and independent clean P0-P2 review pass. Contract `0.1.0`/wire `1`, schema 9, bundle hash, and SDK locks are unchanged.

The bounded user-limit-override milestone passes at core `b2fdfe330175ea8c023ce2af6d8693437010b348`, split across isolated contract correction `68fa1ba28a80cd3fb1e50dffdefc7de935da9f4c`, transactional runtime enforcement `6ae461a50da97ffe48d32359700ddcc3a21fd9b6`, and Admin API/CLI management plus aggregate evidence `b2fdfe330175ea8c023ce2af6d8693437010b348`. Override documents are strict and bounded; only a scoped administrator with `activate_configuration` can replace or clear one. Writers lock the user before the environment, take time after lock waits, heal expired rows, audit exact no-op PUT/DELETE successes, attribute denials, and leave exactly one active row under concurrent replacement. Session authorization coherently seals the exact active override or absence; access policy remains authoritative and an override can select only an existing limit plan, never access or route. Request enforcement uses the sealed selection. Read-only quota snapshots revalidate the sealed override/absence in the same repeatable-read snapshot as configuration and counters before projection, and configuration activation/rollback refuses to remove a referenced plan. Full PostgreSQL normal/race suites, full vet, contract validation, all eight fuzz-smoke targets, focused authorization/configuration/quota/Admin/CLI proof, the established authenticated Chat E2E remaining green, two byte-identical contract `0.2.0` bundles at SHA-256 `a4b320906d1bb02712451224c2111d3a673b4df24631c2f1de01ca5dfbfd0059`, and independent clean P0-P2 review pass. Wire protocol remains `1` and database schema remains `9`; this is local draft evidence, not release readiness, live SDK compatibility, or native-attestation proof.

The dormant hard UTC-calendar input/total-token lifecycle milestone passes at core `c56612b3d9828b3e02ca6b14a33da95afa04e006`. ADR 0017 records the activation boundary: reservation needs a trusted post-route, physical-model-aware conservative input upper bound, so the OpenAI Chat raw-byte estimate is explicitly ineligible. The quota store now supports positive uniform `input_tokens` and `total_tokens` calendar reservations; overflow-safe relationships in which total is at least each present component and equals the checked input-plus-output sum when all three reservations are present; atomic mixed-rule denial; known-success settlement and refund; conservative failed, cancelled, timed-out, unknown, and dispatched-expiry charging; predispatch and undispatched-expiry refund; exact replay; bounded recovery; stable unknown provenance; and read-only pristine/populated/corrupt snapshots. Client projection recognizes those dormant calendar metrics, and relay normalization rejects inconsistent known totals. Production configuration and request routing still reject input/total rules; input/total token buckets and per-request rules remain unsupported. Full PostgreSQL normal/race suites, full vet, contract validation, all eight fuzz-smoke targets, focused lifecycle/snapshot/contention/corruption proof, zero `api`/migration diff, two byte-identical unchanged bundles at SHA-256 `a4b320906d1bb02712451224c2111d3a673b4df24631c2f1de01ca5dfbfd0059`, and independent clean P0-P2 review pass. Contract `0.2.0`, wire protocol `1`, database schema `9`, and all SDK locks remain unchanged; this is dormant internal capability evidence, not production input/total enforcement or release readiness.

## Immediate execution sequence

1. Complete multi-attempt quota persistence so logical request counts remain single-charge while every dispatched attempt and billed cost is durable and conservatively settled.
2. Complete the endpoint-correct protocol registry, then add Responses, Embeddings, Anthropic Messages, restricted opaque routes, deterministic route plans, configured fallback/retries, timeouts, and circuit observations without retry after response commitment.
3. Add production App Attest and Play Integrity server verification and run the cross-repository conformance matrix at the synchronized locks without claiming externally blocked physical evidence early.
4. Complete control-plane resources, observability/jobs, deployment smoke tests, and operational recovery gates.
5. Finish load/soak/upgrade/security hardening and produce signed, published release artifacts only after every release gate passes.
6. Update `STATUS.md` after each material change and keep `COMPLETION_REPORT.md` evidence-only.

## Release slices

- `v0.1.0`: PostgreSQL, debug attestation, custom JWT, DPoP session, OpenAI Chat proxy, request limits, OpenRouter self-test, JavaScript fetch client, Compose.
- `v0.2.0`: identity presets, native attestation, refresh rotation, revocation, Swift and Android alpha.
- `v0.3.0`: remaining protocols, routing, fallback, token/cost reservation, pricing and provenance.
- `v0.4.0`: Admin API, CLI, dashboard, revision management, simulation, audit and usage views.
- `v0.5.0`: all SDK betas and compatibility automation.
- `v0.9.0`: deployments, hardening, upgrade tests and release automation.
- `v1.0.0`: only after the entire Definition of Done passes with published artifacts.
